# Rebuild Wheel: React

Re-implement React:

- `createElement` function
- `render` function
- `Component` class

## How to Run

Install dependencies:

```bash
npm install
```

Start webpack server:

```bash
npm run start
```

Go to http://localhost:8080/
